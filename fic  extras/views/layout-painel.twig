<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Painel{% endblock %}</title>

    <link rel="stylesheet" href="{{ asset('css/painel.css') }}">
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>

<body class="layout">

    <!-- SIDEBAR -->
    <aside class="sidebar">
        <div class="sidebar-header">
            <span class="logo">📁 Loja Virtual</span>
        </div>

        <nav class="menu">
            <a href="{{ url('/painel') }}" class="menu-item">
                <span class="icon">🏠</span> <span class="label">Dashboard</span>
            </a>

            <a href="{{ url('/utilizadores') }}" class="menu-item">
                <span class="icon">👤</span> <span class="label">Utilizadores</span>
            </a>

            <a href="{{ url('/documentos') }}" class="menu-item">
                <span class="icon">📄</span> <span class="label">Documentos</span>
            </a>

            <a href="{{ url('/perfis') }}" class="menu-item">
                <span class="icon">🔐</span> <span class="label">Perfis</span>
            </a>

            <a href="{{ url('/logs') }}" class="menu-item">
                <span class="icon">📝</span> <span class="label">Logs</span>
            </a>
        </nav>
    </aside>

    <!-- MAIN AREA -->
    <div class="main">

        <!-- TOPBAR -->
        <header class="topbar">
            <button class="btn-toggle" onclick="document.body.classList.toggle('sidebar-collapsed')">
                ☰
            </button>

            <div class="topbar-right">
                <span class="user">{{ usuario_nome }}</span>
                <a href="{{ url('/logout') }}" class="logout">Sair</a>
            </div>
        </header>

        <!-- CONTENT -->
        <main class="content-area">
            {% include 'partials/flash.twig' %}
            {% block content %}{% endblock %}
        </main>

    </div>

</body>
</html>