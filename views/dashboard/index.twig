{% extends "base.twig" %}

{% block title %}{{ titulo }}{% endblock %}

{% block content %}
<h1 class="mb-4">Dashboard</h1>

<div class="row">

    <div class="col-md-4">
        <div class="card text-bg-primary mb-3">
            <div class="card-body">
                <h5 class="card-title">Total de Usuários</h5>
                <p class="card-text fs-3">{{ stats.total_usuarios }}</p>
            </div>
        </div>
    </div>

    <div class="col-md-4">
        <div class="card text-bg-success mb-3">
            <div class="card-body">
                <h5 class="card-title">Perfis Criados</h5>
                <p class="card-text fs-3">{{ stats.total_perfis }}</p>
            </div>
        </div>
    </div>

    <div class="col-md-4">
        <div class="card text-bg-warning mb-3">
            <div class="card-body">
                <h5 class="card-title">Registos de Log</h5>
                <p class="card-text fs-3">{{ stats.total_logs }}</p>
            </div>
        </div>
    </div>

</div>

<p class="mt-4">Mais estatísticas e gráficos serão adicionados em breve.</p>
{% endblock %}
