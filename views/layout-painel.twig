<!DOCTYPE html>
<html lang="pt">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Painel{% endblock %}</title>

    <link rel="stylesheet" href="/assets/css/painel.css">
</head>
<body>

    <div class="topbar">
        <div class="topbar-title">Painel de Gestão</div>
        <div class="topbar-user">
            {{ usuario_nome }}
            <a href="{{ url('/logout') }}" class="logout-link">Sair</a>
        </div>
    </div>

    <div class="container">

        <div class="sidebar">
            <ul class="menu">
                <li><a href="{{ url('/painel') }}"><span class="icon">🏠</span> Dashboard</a></li>
                <li><a href="{{ url('/utilizadores') }}"><span class="icon">👤</span> Utilizadores</a></li>
                <li><a href="{{ url('/seguranca') }}"><span class="icon">🔒</span> Segurança</a></li>
                <li><a href="{{ url('/logs') }}"><span class="icon">📄</span> Logs</a></li>
            </ul>
        </div>

        <div class="content">
            {% include 'partials/flash.twig' %}
            {% block content %}{% endblock %}
        </div>

    </div>

</body>
</html>